#include <bits/stdc++.h>
using namespace std;

int main() {
  int n,m,q;
  cin >> n>>m>>q;
  vector<int> a(q), b(q), c(q), d(q);
  vector<int> A(n);
  for (int i = 0; i < q; i++) cin >> a[i] >> b[i] >> c[i] >> d[i];
  for (int i = 0; i < q; i++)
  {
    A[b[i]]-a[a[i]]
  }
  
}
