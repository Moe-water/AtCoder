#include <bits/stdc++.h>
using namespace std;

int main() {
  int count;
  cin >> count;
  vector<int> x(count), y(count), c(count);
  for (int i = 0; i < count; i++) {
    cin >> x[i] >> y[i];
    // c[i] = i;
  }

  for (int i = 0; i < count; i++) {
    double a1 = (y[j] - y[i]) / (x[j] - x[i])
    double a2 = (y[k] - y[i]) / (x[k] - x[i])
  }
}
