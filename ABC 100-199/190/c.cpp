#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, k;
  cin >> n >> m;
  vector<vector<int>> ab(2, vector<int>(n));
  for (int i = 0; i < n; i++) cin >> ab[0][i] >> ab[1][i];
  cin >> k;
  vector<vector<int>> cd(2, vector<int>(k));
  for (int i = 0; i < k; i++) cin >> cd[0][i] >> cd[1][i];

  for (int i = 0; i < k; i++) {
    set<int> k={0};
    k.insert(a[i][])
  }
}
